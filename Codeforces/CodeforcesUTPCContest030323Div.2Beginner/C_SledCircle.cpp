// time-limit: 1000
// problem-url: https://codeforces.com/gym/104217/problem/C
#include <bits/stdc++.h>
#include <ios>
#include <vector>

using namespace std;

void solve(){
    int n;
    cin>>n;
    vector<int> dogPosition(n);
    for(int &dog: dogPosition){
        cin>>dog;
    }
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    solve();
    return 0;
}